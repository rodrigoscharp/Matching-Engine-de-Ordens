global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "athena"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          # Management port, not the API port — see `management.server.port` in application.yml
          - "host.docker.internal:9081"
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: "athena-local"
